
# methodo:
1. first index image each transect to gpkg
    - `index_telops.py` for telpops.
    - `index_flir.py` for flir.

2. plot telops images
    - `plot_ir.py` with frame id selected for each transect using abvoe index. plot `f1` and `f5` in tif.

3. move to `/home/paugam/Src/orthority/mysrc` to run orthorectification
